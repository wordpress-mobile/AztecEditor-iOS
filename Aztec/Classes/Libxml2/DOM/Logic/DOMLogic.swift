extension Libxml2 {
    /// Groups DOM logic.
    ///
    class DOMLogic {
        let rootNode: RootNode

        init(with rootNode: RootNode) {
            self.rootNode = rootNode
        }
    }
}
