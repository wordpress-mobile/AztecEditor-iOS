import Foundation

public enum Libxml2 {
    public enum In {
        typealias HTML = Libxml2.HTML
    }

    public enum Out {
        typealias HTML = Libxml2.HTML
    }

    public enum HTML {
    }
}